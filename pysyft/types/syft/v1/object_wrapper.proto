syntax = "proto3";

package pysyft.types.syft.v1;

import "pysyft/types/torch/v1/script_module.proto";

message ObjectWrapper {
    oneof arg {
        int32 arg_int = 1;
        pysyft.types.torch.v1.ScriptModule script_module = 2;
        // TODO: Cover the case where this contains a Python function
        // How does/will that work when the workers aren't running Python?
    }
}
